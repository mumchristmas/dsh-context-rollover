node_modules/
lib/
# Benchmark libraries are source, not compiled output.
!bench/lib/
!bench/lib/**
tsconfig.dsh-paths.json
tsconfig.build-paths.json
tsconfig.compat-paths.json

# Local working notes and scratch output
audit/
audit-workspace/

# Isolated benchmark runtime, fixtures, and session artifacts (never production state)
.bench/
.bench-test-*/

# Release artifacts are attached to a release, not committed.
*.tgz
